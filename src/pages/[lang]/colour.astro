---
import Layout from "../../layouts/Layout.astro";
import ColourPage from "../../components/ColourPage.astro";
import { localePaths } from "../../lib/i18n.ts";

export const getStaticPaths = localePaths;

const { locale } = Astro.props;
---

<Layout locale={locale} page="colour">
  <ColourPage locale={locale} />
</Layout>
